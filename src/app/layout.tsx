import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import Navbar from "@/components/layout/Navbar";
import Footer from "@/components/layout/Footer";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "CraftedClicks | Professional Web Design & Development Agency",
  description: "We design and develop modern, fast, and conversion-focused websites for companies that want more leads.",
  openGraph: {
    title: "CraftedClicks | Web Design Agency",
    description: "Modern, responsive websites for business growth.",
    url: "https://craftedclicks.com",
    siteName: "CraftedClicks",
    locale: "en_US",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${inter.variable}`}>
      <body className="antialiased min-h-screen flex flex-col">
        <Navbar />
        <main className="flex-grow pt-20">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
